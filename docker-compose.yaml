services:
  qualibur:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./dist/:/var/www/html/
    ports: 
      - 3000:80
    environment:
      - NGINX_HOST=localhost:3000
      - NGINX_PORT=80